- 日本語で回答してください。

## CLAUDE.mdの方針

- このファイルはできるだけシンプルに保つ
- 詳細なルールは `.claude/rules/` に分離する（パス固有ルールで必要時のみ読み込まれる）
- スキル固有の情報は `.claude/skills/` のスキル定義に記載する（スキル呼び出し時のみ読み込まれる）

## GitHub IssueとPRの管理
- IssueやPR、コメントを作成する際は以下を守ること
  - 末尾にフッターを付与する
    ```
    ---
    🤖 Generated with [Claude Code](https://claude.ai/claude-code)
    ```
  - 作成内容に不備がないか作成後に確認すること（例: Issueのレイアウトが崩れている、フッターが付与されていないなど）

## monologueによる思考プロセスの通知

作業中の思考プロセスや状況をユーザーが把握できるよう、適宜 `base-tools:monologue` スキルで独り言を呟くこと。

特に以下のタイミングでは必ず monologue を呼ぶこと:
- 作業開始時・終了時
- ユーザーへの確認が必要な場面（破壊的操作の前、重要な判断の前など）
- 想定外の事態が発生したとき
