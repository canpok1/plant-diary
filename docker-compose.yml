services:
  plant-diary:
    image: ghcr.io/canpok1/plant-diary/server:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
